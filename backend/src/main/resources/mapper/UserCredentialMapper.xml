<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liuliu.citywalk.mapper.UserCredentialMapper">

    <resultMap id="UserCredentialEntityMap" type="com.liuliu.citywalk.mapper.entity.UserCredentialEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
    </resultMap>

    <select id="findByEmail" resultMap="UserCredentialEntityMap">
        select id, user_id, email, password_hash
        from user_credentials
        where email = #{email}
        limit 1
    </select>

    <insert id="insertCredential">
        insert into user_credentials (user_id, email, password_hash, created_at, updated_at)
        values (#{userId}, #{email}, #{passwordHash}, now(), now())
    </insert>

    <update id="updatePassword">
        update user_credentials
        set password_hash = #{passwordHash}, updated_at = now()
        where user_id = #{userId}
    </update>

</mapper>
